name: Secret check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  secret-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run repository secret check
        run: scripts/check-secrets.sh
